Directory structure of SVN tree:

- configfiles
Contains complete configuration files used for testing only, not included in complete builds. Those are generated by the parser.

- doc
Random documentation files not included in the build, includes stuff like base files for the logo

- Files
the filesystem that is added to the image generated by NanoBSD, files in here will be included in the complete build (only place files that are required here)

- Ui
WIP GUI mockup / working set, merged into Files
